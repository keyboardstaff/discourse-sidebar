<div id="scroll-content" class="scroll-content">
  <div class="contents">
    <div class="sidebar"> 
      <div class="sidebar-left"> 
        <div class="sidebar-fix"> 
            {{#if category.uploaded_logo.url}}
              <div class="catimg">{{cdn-img src=category.uploaded_logo.url class="cat-big"}}</div>
              {{#if category.description}}
                 <span class="dis-info-name">{{{dir-span category.name}}}</span>
                 <span class="dis-info">{{{dir-span category.description}}}<br><br>{{i18n "topic_count"}}: <span class="num-info">{{{dir-span category.topic_count}}}</span></span>
        <span class="dis-info-more"><a href="{{{dir-span category.topic_url}}}"><i class="fa fa-hashtag" aria-hidden="true"></i>{{i18n "topic_url"}}</a></span>
              {{/if}}
            {{/if}}
         
             {{cat-info src=category.categorization}}
        </div> 
      </div>
    </div>
    <div class="main-contents">
      <div class="container list-container has-sidebar">
        <div class="list-controls">
          <div class="container">
            {{outlet "navigation-bar"}}
          </div>
        </div>        
        <div class="row">
            <div class="full-width">
              {{conditional-loading-spinner condition=loading}}
              <div id="header-list-area">
                {{outlet "header-list-container"}}
              </div>
            </div>
        </div>
        <div class="row {{if loading "hidden"}}">
          <div class="full-width">
            <div id="list-area">
              {{plugin-outlet name="discovery-list-container-top"
                              args=(hash category=category listLoading=loading)}}
              {{outlet "list-container"}}
            </div>
          </div>
        </div>
      </div>
     </div>
  </div>
{{plugin-outlet name="discovery-below"}}
</div>